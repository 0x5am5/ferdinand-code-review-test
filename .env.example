SESSION_SECRET=

# Local PostgreSQL Database (for local development)
DATABASE_URL=postgresql://username@localhost:5432/ferdinand_dev

# OR use a remote Neon Database
# DATABASE_URL=postgresql://user:password@host.region.aws.neon.tech/database?sslmode=require

FIREBASE_PROJECT_ID=
FIREBASE_PRIVATE_KEY=
FIREBASE_CLIENT_EMAIL=

POSTMARK_API_KEY=
POSTMARK_FROM_EMAIL=

ADOBE_FONTS_API_KEY=
GOOGLE_FONTS_API_KEY=

# Slack Integration (Phase 0)
SLACK_BOT_TOKEN=your-slack-bot-token-here
SLACK_SIGNING_SECRET=your-slack-signing-secret-here
SLACK_CLIENT_ID=your-client-id
SLACK_CLIENT_SECRET=your-client-secret

# OpenAI Integration (for natural language processing)
OPENAI_API_KEY=your-openai-api-key-here

ENCRYPTION_KEY=entryption-key

# Replit variables
REPL_ID=3fc3a052-7343-4b9f-9388-7eb42ba83b7a

NODE_ENV=development
PORT=3001
APP_BASE_URL=https://your-actual-domain.com

# Local Development Auth Bypass (NEVER enable in production!)
# When set to "true", bypasses Firebase authentication and auto-logs in as DEV_USER_EMAIL
BYPASS_AUTH_FOR_LOCAL_DEV=false
DEV_USER_EMAIL=

# File Storage Configuration
# Options: local, s3, r2 (default: local)
STORAGE_TYPE=local

# Local Storage (when STORAGE_TYPE=local)
STORAGE_PATH=./uploads

# Cloudflare R2 Storage (when STORAGE_TYPE=r2)
# See docs/R2-STORAGE-SETUP.md for complete setup instructions
# R2_BUCKET=ferdinand-assets
# R2_ACCOUNT_ID=your-cloudflare-account-id
# R2_ACCESS_KEY_ID=your-r2-access-key-id
# R2_SECRET_ACCESS_KEY=your-r2-secret-access-key
# R2_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com
# R2_REGION=auto
# Optional: Custom domain for public assets
# R2_PUBLIC_URL=https://assets.yourdomain.com

# AWS S3 Storage (when STORAGE_TYPE=s3)
# S3_BUCKET=your-s3-bucket
# S3_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-aws-access-key-id
# AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key